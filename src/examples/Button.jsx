import styled from "styled-components"

const color = "blue"
const Button = styled.button`
  color: white;
  background: ${color};
  font-size: 1rem;
  font-weight: 300;
  margin-left: 4px;
  padding: 0.8rem;
`

export default Button
